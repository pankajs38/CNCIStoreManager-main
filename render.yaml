services:
  - type: static_site
    name: cncistoremanager-main
    env: static
    buildCommand: npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /auth/callback
        destination: /index.html
      - type: rewrite
        source: /*
        destination: /index.html
